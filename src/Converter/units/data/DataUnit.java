package Converter.units.data;

import Converter.IUnit;

public enum DataUnit implements IUnit {
    BIT,
    BYTE,
    KILOBYTE,
    MEGABYTE,
    GIGABYTE,
    TERABYTE,
    KILOBIT,
    MEGABIT,
    GIGABIT,
}
