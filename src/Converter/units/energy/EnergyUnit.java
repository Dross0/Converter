package Converter.units.energy;

import Converter.IUnit;

public enum EnergyUnit implements IUnit {
    CALORIES,
    KILOCALORIES,
    KILOWATT_PER_HOUR,
    BTU,
    ERG,
    JOULE,
    KILOJOULE,
}
