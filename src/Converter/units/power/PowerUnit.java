package Converter.units.power;

import Converter.IUnit;

public enum PowerUnit implements IUnit {
    HORSE_POWER,
    WATT,
    KILOWATT,
    CAL,
    KILOCAL,
    BTU_PER_HOUR,
    BTU_PER_SECOND
}
