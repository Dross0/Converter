package Converter.units.speed;

import Converter.IUnit;

public enum SpeedUnit implements IUnit {
    KILOMETER_PER_HOUR,
    METER_PER_SECOND,
    MILE_PER_HOUR,
    FOOT_PER_SECOND,
    KNOT,
    SPEED_OF_LIGHT,
    MACH
}
